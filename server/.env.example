# Porta HTTP opcional (padrão: 4000)
# PORT=4000

# URL de conexão utilizada pelo Prisma (obrigatória)
DATABASE_URL="postgresql://user:password@localhost:5432/amadelivery"

# Segredo obrigatório utilizado na assinatura de tokens JWT
JWT_SECRET="altere-este-valor-para-um-segredo-seguro"

# Tempo de expiração do token JWT (opcional, padrão: 1h)
# JWT_EXPIRES_IN=1h

# Origens permitidas pelo CORS separadas por vírgula
# ALLOWED_ORIGINS=http://localhost:5173,https://app.exemplo.com

# Configurações do rate limit de autenticação
# AUTH_RATE_LIMIT_WINDOW_MS=900000
# AUTH_RATE_LIMIT_MAX=10

# Nível mínimo de logs: debug, info, warn ou error (opcional)
# LOG_LEVEL=info
